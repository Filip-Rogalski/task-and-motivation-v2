function newDateToAppDate() {
    let date = new Date(),
        year = date.getFullYear(),
        month = date.getMonth(),
        day = date.getDate();
    
    return ;
}

export default newDateToAppDate;